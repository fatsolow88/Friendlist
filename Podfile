source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '10.0'
use_frameworks!

target 'Friendlist' do
  pod 'Alamofire', '~> 4.0'
  pod 'PKHUD', '~> 5.0'
  target 'FriendlistTests' do
    inherit! :search_paths

  end
end
